export default function Loading() {
   // You can add any UI inside Loading, including a Skeleton.
   return (
      <div className='min-h-screen w-full flex items-start justify-start dark:bg-zinc-800 bg-white'>
         <span className='dark:text-gray-200 text-'>Loading...</span>
      </div>
   );
}
